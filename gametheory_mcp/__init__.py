"""
gametheory-mcp — equilibrium-aware primitives for AI agents over MCP + library.

The math primitives (Rubinstein, Myerson, Gale-Shapley, Bayesian particle
filter) ship under Apache 2.0. The hosted API at https://api.snhp.dev adds
the empirical-priors corpus (warm-starts new agents per vertical) and the
first-strike cryptographic commit-reveal flow that requires server-side
EdDSA signing.
"""

__version__ = "0.1.0"
